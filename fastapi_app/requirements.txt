annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.0
certifi==2025.11.12
charset-normalizer==3.4.4
click==8.3.1
colorama==0.4.6
fastapi==0.128.0
h11==0.16.0
idna==3.11
pydantic==2.12.5
pydantic_core==2.41.5
requests==2.32.5
setuptools==80.9.0
starlette==0.50.0
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.6.2
uvicorn==0.40.0
wheel==0.45.1
psycopg2-binary  # DB 접속용
sqlalchemy       # ORM

# [추가] NumPy 버전 고정 (Torch 및 Transformers 호환성 보장)
numpy<2.0

# [최적화] CPU 버전의 PyTorch 다운로드 경로 지정
--extra-index-url https://download.pytorch.org/whl/cpu

# CPU 전용 버전 명시 (버전 뒤에 +cpu 필수)
torch==2.2.2+cpu


 # ★ 핵심: 텍스트를 벡터로 바꿔주는 AI 모델 이제 위에서 설치된 가벼운 torch를 사용함  ❌ (번역용 아님 RAG / 유사도 검색용)
sentence-transformers 


pgvector         # DB 벡터 연산 지원
httpx            # 비동기 HTTP 클라이언트 (외부 API 호출용)
python-dotenv    # 환경 변수 관리


# 번역용 AI 모델 (Moved to fastapi_ai_translation)
# transformers  
# torch   
# sentencepiece  
# huggingface-hub

prometheus-fastapi-instrumentator #부하 테스트용
