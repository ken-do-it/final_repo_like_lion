name: Build and Deploy

on:
  push:
    branches: ["develop"]

# 환경변수 설정 (이미지 이름)
env:
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  DJANGO_IMAGE: korea-trip-be
  FASTAPI_IMAGE: korea-trip-ai
  FRONTEND_IMAGE: korea-trip-fe

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 1. Django 빌드 & 푸시 (변경된 경우에만)
      - name: Build & Push Django
        uses: docker/build-push-action@v4
        with:
          context: ./django_app
          file: ./django_app/Dockerfile
          push: true
          tags: ${{ env.DOCKER_USERNAME }}/${{ env.DJANGO_IMAGE }}:latest

      # 2. FastAPI 빌드 & 푸시
      - name: Build & Push FastAPI
        uses: docker/build-push-action@v4
        with:
          context: ./fastapi_app
          file: ./fastapi_app/Dockerfile
          push: true
          tags: ${{ env.DOCKER_USERNAME }}/${{ env.FASTAPI_IMAGE }}:latest

      # 3. Frontend (React) 빌드 & 푸시
      # (Dockerfile에서 npm run build 하고 nginx로 서빙하는 멀티스테이지 빌드 추천)
      - name: Build & Push Frontend
        uses: docker/build-push-action@v4
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: ${{ env.DOCKER_USERNAME }}/${{ env.FRONTEND_IMAGE }}:latest
          build-args: |
            VITE_GOOGLE_MAPS_API_KEY=${{ secrets.VITE_GOOGLE_MAPS_API_KEY }}
            VITE_API_BASE_URL=${{ secrets.VITE_API_BASE_URL }}
            VITE_SEARCH_API_URL=${{ secrets.VITE_SEARCH_API_URL }}
            VITE_PLACES_API_URL=${{ secrets.VITE_PLACES_API_URL }}
            VITE_KAKAO_JS_KEY=${{ secrets.VITE_KAKAO_JS_KEY }}
            VITE_TOSS_CLIENT_KEY=${{ secrets.VITE_TOSS_CLIENT_KEY }}
      
      - name: Build & Push FastAPI Places
        uses: docker/build-push-action@v4
        with:
          context: ./fastapi_places
          file: ./fastapi_places/Dockerfile
          push: true
          tags: ${{ env.DOCKER_USERNAME }}/korea-trip-places:latest

      - name: Build & Push FastAPI Translation
        uses: docker/build-push-action@v4
        with:
          context: ./fastapi_ai_translation
          file: ./fastapi_ai_translation/Dockerfile
          push: true
          tags: ${{ env.DOCKER_USERNAME }}/korea-trip-translation:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop'
    steps:
      - name: Deploy to EC2 via SSH
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          script: |
            cd ~/final_repo
            
            # 최신 이미지 pull
            sudo docker compose pull
            
            # 컨테이너 재시작
            sudo docker compose up -d
            
            # 안 쓰는 이미지 정리 (선택)
            sudo docker image prune -f
            
            echo "Deployment Finished! 🚀"
